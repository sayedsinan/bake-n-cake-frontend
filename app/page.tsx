import styles from "./login.module.css";

export default function Login() {
  return (
    <main className={styles.main}>
      <div className={styles["login-box"]}
      >
        <h2 className={styles["heading"]}>Login</h2>
        <h2 className={styles["heading-2"]}>Login</h2>
      </div>
    </main>
  );
}
